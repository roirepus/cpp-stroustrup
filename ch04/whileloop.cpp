#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main(int argc, char *argv[]) {
  vector<double> temps;
  for (double temp; cin >> temp;) {
    temps.push_back(temp);
  }
  return 0;
}
