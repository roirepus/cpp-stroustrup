#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
  string first;
  string second;
  cin >> first >> second;
  if (first > second) {
    cout << first << " is alphabetically after " << second;
  }

  return 0;
}
